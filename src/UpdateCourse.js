import React, { Component } from 'react';
import { Button } from 'reactstrap';

class Update extends Component {
  render() {
    return (
    <Button className="Update">Edit Course</Button>
    );
  }
}
export default Update;
